node_modules/
dist/
.env
data/
*.sqlite
*.sqlite3
*.log
.DS_Store
# Packaged on demand with `npm run package-teams-app` from manifest.json + icons/,
# which ARE committed - not committing the zip avoids it going stale next to them.
teamsapp/teamsapp.zip
